# Rollback Alpha Lab LCM-10C

Revert only paths listed in `LCM_10C_FILE_INDEX.txt`, restore the exact LCM-10B handoff baseline, verify the upstream output manifest and rerun the LCM-10B direct tests. Do not delete or modify legacy sources as part of rollback.
