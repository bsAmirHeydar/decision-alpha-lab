# Install — Alpha Lab LCM Roadmap Refinement R1

Install at the repository root. Verify the ZIP SHA-256 before expansion. After expansion, run package validation, Python compilation, direct roadmap tests and LCM-07 regression before staging the exact file index.

Roadmap root: `registry/legacy_context_migration/roadmaps/LCM_ROADMAP_R1_BALANCED_PARTITION`

This patch is documentation and governance only. It does not advance LCM-08A implementation state.
