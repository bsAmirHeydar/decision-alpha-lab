# Alpha Lab RTHP ACL-03 Compilation and Onboarding Patch

This patch advances `CTX_RTHP_CROSS_SYMBOL_CYCLE_DIVERGENCE_V1` from ACL-02 `INTAKE_COMPLETE` to ACL-03 `CONTEXT_COMPILED`.

It closes independent review, adds exact generic occurrence-identity aliases, revalidates ACL-02, freezes the source, compiles all ACL-03 IR artifacts, executes golden replay, emits least-privilege adapters, creates bounded SAED/UCEE bindings, pins an MQL5 static diagnostic mirror, and publishes the ACL-04 handoff.

It does not implement runtime adapters, train models, define Setup/Treatment, submit orders, or activate capital.
