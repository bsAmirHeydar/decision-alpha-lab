# EXP0019 FP-I12 Operator UX

Implements the complete operator-facing indicator layer: panel, projection-only filters, deduplicated alerts, append-only CSV/JSONL audit export, safe actions, preferences, checkpoints, diagnostics, and an MQL5 self-test indicator.

Live execution authority remains `NONE`. `FP-DEC-012` remains `UNSET` and visible in the panel.
