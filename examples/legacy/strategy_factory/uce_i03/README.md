# UCE-I03 Examples

- `manual_wide_stop_fixed_target.json` encodes a personal wide-stop/fixed-target style.
- `ai_tight_stop_runner.json` encodes an AI-search tight-stop/runner style.

Both use identical exact-version atom contracts. They are selections, not complete compiled treatments or broker orders.
