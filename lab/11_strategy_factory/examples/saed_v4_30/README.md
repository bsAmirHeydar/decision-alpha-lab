# V4-30 synthetic reference inputs

These inputs bind the exact V4-29 certificate and handoff to a frozen replication protocol, package commitment, three logically independent synthetic laboratory identities, three default-deny synthetic environments and a deterministic aggregate-only reference payload. They are control fixtures, not external replication evidence.
