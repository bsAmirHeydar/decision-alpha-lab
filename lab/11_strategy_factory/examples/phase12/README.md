# Phase 12 examples

These fixtures demonstrate an anchored walk-forward plan, immutable optimization trial ledger, explicit parameter-neighbor graph, deterministic stress registry and an evidence-only promotion decision. Replace placeholder hashes with hashes generated from canonical artifacts before any formal validation run.
