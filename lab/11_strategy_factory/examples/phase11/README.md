# Phase 11 Reference Example

`statistical_samples.csv` is a deterministic, non-trading fixture used to exercise grouped summaries, confidence intervals, matched-null assignment, report generation and artifact hashing. It does not represent evidence of a market edge.
