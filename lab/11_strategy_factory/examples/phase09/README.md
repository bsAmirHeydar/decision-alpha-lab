# Phase 09 Examples

The examples pin a conservative simulation policy and a deterministic fixed-cost model. Runtime-generated hashes must be persisted in every outcome run manifest. These files are examples, not permission to execute orders.
