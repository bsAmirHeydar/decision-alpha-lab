# Context Package Agent Rules

- Do not change Context semantics without a versioned amendment.
- Never write generated files manually.
- Never place credentials, raw restricted data or model secrets in the repository.
- Preserve known-time semantics and fail closed on ambiguity.
- Human and AI Setups compile to the same canonical IR.
- No package can authorize capital or bypass security policy.
