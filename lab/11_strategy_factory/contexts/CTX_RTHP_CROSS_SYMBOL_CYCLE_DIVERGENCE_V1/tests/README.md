# RTHP Context Tests

Run `python -m pytest -q lab/11_strategy_factory/tests/rthp_context`. The suite validates package completeness, detector semantics, fixture parity, lifecycle transitions and the MQL5 authority boundary.
