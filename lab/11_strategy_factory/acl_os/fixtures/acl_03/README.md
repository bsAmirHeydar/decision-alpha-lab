# ACL-03 Fixtures

The valid Context fixture is an ACL-02 package enriched with deterministic event sequences. Negative fixtures are generated by tests through controlled mutations so every guard is exercised against the same canonical source.
