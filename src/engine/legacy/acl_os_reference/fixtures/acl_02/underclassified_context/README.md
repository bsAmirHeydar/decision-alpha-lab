# ACL-02 Context Package Template

This directory is copied by `python -m src.engine.tooling.strategy_factory.acl_os.acl_02.cli scaffold CTX_ID`. Authored files are mutable only through reviewed semantic amendments. `generated/` is compiler-owned.
