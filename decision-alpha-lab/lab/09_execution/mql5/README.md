# MQL5 Execution Runtime

The active runtime for Decision Alpha Lab is now MQL5-native.

Python bridges, external watchers, FastAPI services, React UI, Parquet caches, and CSV visual adapters are not part of the active execution path.

Primary Expert:

```text
mql5/Experts/DecisionAlphaLab/M0001/M0001_LiveVisualLab.mq5
```

Core Includes:

```text
mql5/Include/DecisionAlphaLab/
```

Validation export script:

```text
mql5/Scripts/DecisionAlphaLab/M0001/M0001_ExportValidationJournal.mq5
```
