# Install EXP0019 FP-I09 Ledger Patch

Apply only after FP-I08. Expand the ZIP in the repository root, remove the ZIP from the root, stage only paths in `EXP0019_FP_I09_FILE_INDEX.txt`, run Python/static validation, compile both MQL5 entry points locally, then commit and push.
