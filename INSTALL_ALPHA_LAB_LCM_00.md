# Installation

Apply the root-relative patch, verify baseline preservation, run LCM-00 tests, commit, create the annotated baseline tag, and push both commit and tag. Use only the supplied pathspec file for staging.

## Commit file

Use `COMMIT_MESSAGE_LCM_00.md`. The existing repository commit-message artifact is not overwritten by this phase.
