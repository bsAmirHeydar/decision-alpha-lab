# Install FP-I12

Extract the patch at the repository root. Stage only paths listed in `EXP0019_FP_I12_FILE_INDEX.txt`. Compile the production indicator and `EXP0019_FP_I12_OperatorUXSelfTest.mq5` in MetaEditor. Validate panel controls, alert channels, and local CSV/JSONL export before entering FP-I13.
