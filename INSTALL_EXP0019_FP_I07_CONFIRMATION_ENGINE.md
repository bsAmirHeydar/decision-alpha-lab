# Install FP-I07

Extract the patch into the repository root. Stage only paths from `EXP0019_FP_I07_FILE_INDEX.txt`. Run the FP-I07 pytest suite and compile both FP-I07 MQL5 entrypoints in local MetaEditor. Roll back only paths listed in the same file index.
