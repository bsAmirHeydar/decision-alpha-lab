# Install LCM-11B

Apply from a clean repository root with the exact PowerShell procedure supplied with the patch. The procedure verifies ZIP SHA-256, expands, validates, tests, stages exact paths, commits and pushes.
