# Installation

Extract at the repository root with overwrite enabled. Run `python tools/strategy_factory/validate_ucee_roadmap.py docs/strategy_factory_universal_context_exploitation_engine` and the repository engineering policy before committing. This is a documentation and implementation-program patch; it grants no live trading authority.
