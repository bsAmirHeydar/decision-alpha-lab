# Install Phase 09

Extract this root-relative patch over a repository that already contains Phases 01–08. Run `tools/strategy_factory/run_phase09_tests.ps1`, then compile and run the SF09 self-test, diagnostic and host EAs in MetaEditor.
