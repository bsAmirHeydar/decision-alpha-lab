# Installation and Verification

Extract at the repository root, run the Phase 13 PowerShell test script, compile the three SF13 Expert Advisors with local MetaEditor, review the generated contracts and fixtures, then commit the patch as one atomic phase delivery. No existing model or runtime pointer is mutated.
