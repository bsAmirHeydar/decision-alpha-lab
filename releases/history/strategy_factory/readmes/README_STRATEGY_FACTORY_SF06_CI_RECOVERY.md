# Strategy Factory SF06 CI Recovery

Corrects the remaining Phase 06 MQL5 compatibility violations and replaces the opaque multi-command CI workflow with one shared local/CI preflight entry point. Apply this patch after the already-committed Phase 06 and SF04/SF05 hotfix commits.
