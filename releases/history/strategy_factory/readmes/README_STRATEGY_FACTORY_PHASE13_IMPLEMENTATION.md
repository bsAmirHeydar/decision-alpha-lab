# Strategy Factory Phase 13

Implements the central, strategy-neutral bridge from accepted anti-overfit evidence to leakage-safe datasets and transparent baseline training. MQL5 defines and exports causal rows. Python performs train-only transforms, deterministic baseline fitting, validation-only selection and calibration, and test-only OOS prediction publication. The patch contains no ONNX, model promotion, paper or live execution authority.
