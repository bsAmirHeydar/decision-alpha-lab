feat(lcm): implement LCM-03 canonical identity alias and locator

Register provisional path-independent identity candidates, explicit identity
ambiguities, scoped legacy aliases, deterministic logical locators, collision
blocks, version rules and the static consumer census over all active LCM-02
migration candidates.

Preserve every legacy source artifact and deny move, deletion, merge, semantic
refactor, cutover, runtime generation, live order and capital authority.
