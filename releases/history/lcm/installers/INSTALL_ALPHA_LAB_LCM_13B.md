# Install Alpha Lab LCM-13B

Apply the ZIP from the repository root with the provided PowerShell command block. The command verifies the ZIP digest, expands the exact patch, runs package/schema/static verification, runs direct and stable regression suites, stages only `LCM_13B_FILE_INDEX.txt`, checks the staged path set, commits and pushes.
