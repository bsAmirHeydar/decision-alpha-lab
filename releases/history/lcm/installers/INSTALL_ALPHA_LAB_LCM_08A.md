# Install Alpha Lab LCM-08A

1. Verify the ZIP SHA-256 before extraction.
2. Expand at repository root.
3. Verify the generated portfolio package and installation.
4. Run direct and regression tests.
5. Stage only paths in `LCM_08A_FILE_INDEX.txt`.
6. Commit with `COMMIT_MESSAGE_LCM_08A.md` and push.

Rollback removes only patch-indexed additions and restores modified documentation from the prior Git commit. No legacy source file is moved or deleted by this patch.
