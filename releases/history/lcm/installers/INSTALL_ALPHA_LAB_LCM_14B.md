# Install Alpha Lab LCM-14B

Apply the ZIP from the repository root with the supplied PowerShell block. The workflow validates the exact ZIP SHA-256 and entry set, expands the payload, removes the ZIP, verifies the hash ledger, schemas, static authority boundary, quarantine package and installation, executes the direct LCM-14B suite and stable regressions, stages only the exact root-relative file index, verifies staged-path equality, commits and pushes.

Installation retains all redirects and canonical documents. It does not delete any artifact or grant runtime, live-order, capital or deletion authority.
