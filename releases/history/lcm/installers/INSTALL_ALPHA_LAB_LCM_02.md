# Install LCM-02

Extract the root-relative patch into the repository root. Locate the generated `CLASSIFICATION_*` package under `registry/legacy_context_migration/classifications`, verify it, verify installation against the exact patch index, compile the LCM-02 Python package, run LCM-02 plus direct LCM-01/LCM-00/ACL-15 regressions, stage only `LCM_02_FILE_INDEX.txt`, commit and push.
