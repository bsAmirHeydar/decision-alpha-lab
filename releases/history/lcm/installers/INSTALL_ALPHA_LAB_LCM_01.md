# Installation

Apply the root-relative patch, verify the immutable LCM-00 input binding, verify the LCM-01 survey package, verify installation paths, run LCM-01 tests and LCM-00 regression tests, then commit and push. Use only `LCM_01_FILE_INDEX.txt` for staging.

## Commit file

Use `COMMIT_MESSAGE_LCM_01.md`.
