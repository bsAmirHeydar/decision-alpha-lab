# Install Alpha Lab LCM-11A

Apply from repository root using the supplied PowerShell block. It verifies the ZIP, expands it, removes the ZIP, validates the package and tests, stages only the exact file index, commits and pushes.
