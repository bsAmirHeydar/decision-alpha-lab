# Install Alpha Lab LCM-13C

Apply the ZIP from the repository root with the supplied PowerShell block. The workflow validates the ZIP digest and entry safety, expands the exact payload, verifies the hash ledger, schemas, static authority boundary, closure package, direct tests and stable regressions, stages only the root-relative file index, verifies exact staged-path equality, commits and pushes.
