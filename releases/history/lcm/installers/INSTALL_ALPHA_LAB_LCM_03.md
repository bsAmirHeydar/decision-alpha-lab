# Install LCM-03

Extract the root-relative patch into the repository root. Verify the deterministic `IDENTITY_A3F7E93C3F5154CB2616F01F5235F748` package, verify installation against the exact patch index, compile the LCM-03 Python package, run LCM-03 plus direct LCM-02/LCM-01/LCM-00/ACL-15 regressions, stage only `LCM_03_FILE_INDEX.txt`, commit and push.

This patch is non-destructive. It does not move or delete legacy source and does not authorize semantic refactoring, merge, cutover, runtime generation, live order submission or capital activation.
