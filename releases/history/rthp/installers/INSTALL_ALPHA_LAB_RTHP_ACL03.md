# Install RTHP ACL-03 Patch

Apply from the repository root. Verify the ZIP digest, expand it, run the RTHP and central ACL validation suites, verify deterministic recompilation, stage exactly the indexed paths, commit, and push.

Expected Context state after installation: `CONTEXT_COMPILED` with open adapter and ACL-04/ACL-05 obligations.
