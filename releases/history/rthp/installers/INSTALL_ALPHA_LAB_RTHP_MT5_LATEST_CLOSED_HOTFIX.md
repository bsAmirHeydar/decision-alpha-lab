# Install

Apply from the repository root only after the RTHP MT5 one-click implementation is installed. Validate the prerequisite file hashes, expand the patch, run the focused and full RTHP MT5 tests, stage only indexed paths, commit, and push.
