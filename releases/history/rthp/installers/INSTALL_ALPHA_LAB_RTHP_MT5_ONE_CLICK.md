# Install RTHP MT5 One-Click Implementation

Apply this patch after the canonical RTHP Context, ACL-03 onboarding, AI-input binding, Real Train Activation, and MT5 automation roadmap patches.

The release PowerShell block verifies the patch hash, exact paths, schemas, boundaries, compilation, and 422 tests before exact staging, commit, and push.

The MetaTrader Python package is intentionally lazy-loaded and is only needed for a real Windows terminal run. Install it from the context-owned requirements file after the patch is committed.
