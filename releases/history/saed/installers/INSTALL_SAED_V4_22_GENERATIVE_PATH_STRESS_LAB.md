# Installation and validation

Expand the patch in the repository root, remove the ZIP, run the full QA and delivery validator, stage exactly the file index, check the staged diff, commit using `COMMIT_MESSAGE.md`, and push.

The MQL5 result is static evidence only until local Windows MetaEditor compilation is attached.
