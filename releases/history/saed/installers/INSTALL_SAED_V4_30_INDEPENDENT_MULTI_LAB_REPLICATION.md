# Installation

Expand the ZIP at repository root, remove it, run full QA and delivery validation, stage only `SAED_V4_30_FILE_INDEX.txt`, inspect the staged diff, commit with `COMMIT_MESSAGE.md`, and push.
