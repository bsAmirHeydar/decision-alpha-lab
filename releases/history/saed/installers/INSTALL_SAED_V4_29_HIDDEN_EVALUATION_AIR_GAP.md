# Installation

Expand the ZIP at the repository root, remove the ZIP, run the V4-29 full QA and delivery validator, stage exactly the paths in `SAED_V4_29_FILE_INDEX.txt`, verify the staged diff, commit with `COMMIT_MESSAGE.md`, and push.
