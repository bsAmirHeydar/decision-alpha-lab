# Install SAED V4-03

Apply this patch at the repository root after SAED V4-02. Stage only paths listed in `SAED_V4_03_FILE_INDEX.txt`. Run phase tests and local MetaEditor compile separately; static MQL5 validation is not compile evidence.
