# Install FP-I15

Expand the patch at the repository root. Stage only paths from `EXP0019_FP_I15_FILE_INDEX.txt`. Run the Python tests and source validators. Compile the Paper EA and self-test locally with MetaEditor. The product is synthetic-only and must not be interpreted as permission for live execution.
