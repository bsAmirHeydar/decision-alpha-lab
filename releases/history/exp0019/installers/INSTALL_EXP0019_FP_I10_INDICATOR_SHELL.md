# Install EXP0019 FP-I10 Indicator Shell

Apply only after FP-I09. Expand the patch in the repository root, remove the ZIP from the root, stage only paths in `EXP0019_FP_I10_FILE_INDEX.txt`, run Python and static gates, compile the production Indicator and self-test Indicator in MetaEditor, then commit and push.
