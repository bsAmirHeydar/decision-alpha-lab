# EXP0019 Faerie Protocol — FP-I13 Indicator Release

FP-I13 proves the complete indicator across historical replay, incremental updates, restart, chart timeframe changes, multiple isolated instances, and resource budgets. It packages four release profiles, a deterministic replay harness, acceptance evidence, an MQL5 release guard, a local MetaEditor compile script, and a complete operator guide.

The source status is `SOURCE_ACCEPTED_EXTERNAL_COMPILE_PENDING`. MetaEditor compilation and terminal runtime acceptance must be performed locally before the production gate is marked fully accepted.
