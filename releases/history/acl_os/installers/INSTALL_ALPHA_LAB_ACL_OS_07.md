# ACL-07 Installation

Expand the patch at repository root. The archive contains root-relative paths and must overlay an installation that already includes ACL-04, ACL-05 and ACL-06.

Run the ACL-07 tests with:

`python -m pytest lab/11_strategy_factory/acl_os/tests_acl_07 -q`
