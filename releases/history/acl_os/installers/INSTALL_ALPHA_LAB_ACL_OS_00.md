# Install ACL-OS ACL-00

Extract the patch into the repository root, remove the ZIP, run full QA and delivery validation, stage the exact file index, inspect the staged diff, commit with `COMMIT_MESSAGE.md`, and push.

The patch assumes the ACL-OS Architecture v1.0.0 baseline is already installed.
