# Install ACL-02

Expand the ZIP at repository root, run the full QA and delivery validator, stage only `ACL_OS_02_FILE_INDEX.txt`, commit with `COMMIT_MESSAGE.md`, and push. ACL-00 and ACL-01 must already be installed.
