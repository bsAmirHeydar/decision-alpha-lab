# Historical Release Evidence

This tree contains historical delivery artifacts relocated from the repository root during UC-03 Part 1. Files are moved byte-for-byte; active references are rewritten through the relocation map. These artifacts are historical evidence, not current architectural authority.
