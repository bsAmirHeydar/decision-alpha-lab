# UC04-W0 — Semantic Unification Foundation and Test Recovery

This release establishes the executable foundation for UC-04 without merging or retiring trading logic.

## Delivered boundaries

- canonical, archive-safe repository path authority;
- repository-wide Pytest collection recovery;
- migration-aware preservation of UC-01 and UC-03 evidence;
- RTHP Context, AI-input, Train Activation and MT5 Activation binding recovery;
- deterministic ACL03 regeneration and registration repair;
- UC-04 governance schemas, machine records, verifier, tests and CI;
- synchronized program status and W1 handoff.

## Accepted evidence

- `9,732` tests collected with `0` collection errors;
- scoped consolidation/RTHP/UC04 suite target: `166 passed`, `7 skipped`;
- no Context, model, Train Engine, MQL5 signal, order or capital semantics changed;
- no deletion or semantic retirement authority.

The next authorized delivery is UC04-W1 characterization and bounded implementation of the deterministic MQL5 formatting primitive.

## Known limitation

The supplied source ZIP contains four historical Git LFS pointers instead of their payloads. Repository-wide collection succeeds, but full execution is recorded as `INCOMPLETE_AT_FIRST_HISTORICAL_LFS_BLOCKER`; tests beyond that first blocker are not claimed as passing.
