# Rollback

Use `git revert <UC-03-Part-1-commit>` after the commit is created. Before committing, use `git reset --hard HEAD` and remove untracked UC-03 Part 1 files if the application is intentionally abandoned.

Historical artifact bytes are preserved at their relocation destinations and recorded in the root relocation receipt.
