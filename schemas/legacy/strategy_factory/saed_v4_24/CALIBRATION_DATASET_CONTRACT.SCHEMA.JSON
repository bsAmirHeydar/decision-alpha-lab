{
  "$id": "https://decision-alpha-lab.local/schemas/saed-v4-24/calibration_dataset_contract.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Closed deterministic research-only contract. Unknown object fields are rejected.",
  "properties": {
    "allowed_roles": {
      "items": {
        "type": "string"
      },
      "type": "array"
    },
    "cluster_role_separation_required": {
      "type": "boolean"
    },
    "exact_version": {
      "type": "string"
    },
    "feature_known_by_decision": {
      "type": "boolean"
    },
    "feature_names": {
      "items": {
        "type": "string"
      },
      "type": "array"
    },
    "future_suffix_forbidden": {
      "type": "boolean"
    },
    "minimum_clusters": {
      "type": "integer"
    },
    "minimum_records": {
      "type": "integer"
    },
    "minimum_records_per_regime": {
      "type": "integer"
    },
    "outcome_after_decision": {
      "type": "boolean"
    },
    "protected_evidence_forbidden": {
      "type": "boolean"
    },
    "required_fields": {
      "items": {
        "type": "string"
      },
      "type": "array"
    },
    "strict_decision_time_order": {
      "type": "boolean"
    }
  },
  "required": [
    "allowed_roles",
    "cluster_role_separation_required",
    "exact_version",
    "feature_known_by_decision",
    "feature_names",
    "future_suffix_forbidden",
    "minimum_clusters",
    "minimum_records",
    "minimum_records_per_regime",
    "outcome_after_decision",
    "protected_evidence_forbidden",
    "required_fields",
    "strict_decision_time_order"
  ],
  "title": "SAED V4-24 CALIBRATION_DATASET_CONTRACT",
  "type": "object",
  "x-authority": "research-only-nonproduction",
  "x-phase": "SAED_V4_24"
}
