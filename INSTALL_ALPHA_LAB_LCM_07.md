# Install Alpha Lab LCM-07

Extract the patch at repository root, verify the package and installation, run direct and regression tests, stage only `LCM_07_FILE_INDEX.txt`, commit with `COMMIT_MESSAGE_LCM_07.md`, then push.
