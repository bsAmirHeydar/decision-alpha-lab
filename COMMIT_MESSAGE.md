feat(alpha-lab): implement deterministic candidate policy engine

Add typed entry, stop and exit policy contracts, exact static policy registration, immutable parameter packets, startup-compiled candidate matrices, admissibility semantics, deterministic geometry construction, stable TradeCandidate identities, bounded candidate queues, telemetry, neutral fixture policies, SF08 host, diagnostic and self-test EAs, Python conformance mirrors, schemas, fixtures, detailed Obsidian documentation, QA and the Phase 09 handoff.
