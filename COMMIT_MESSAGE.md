# Commit Message

```text
feat(acl-os): implement ACL-05 immutable batch and artifact store
```

## Body

- validate the exact ACL04_TO_ACL05 generated bundle and authority permit;
- freeze canonical and diagnostic setup universes without behavior mutation;
- bind known-time-safe dataset snapshots, label maturity, purged splits, environment and budgets;
- publish SHA-256 byte-addressed immutable objects, event ledger, provenance, manifests and receipts;
- emit a bounded ACL05_TO_ACL06 research-DAG handoff;
- add closed schemas, machine policies, Python implementation, tests, static MQL5 mirrors and extensive Obsidian documentation;
- preserve the `RESEARCH_BATCH_FREEZE_REFERENCE_ONLY` claim ceiling and deny order/capital authority.
