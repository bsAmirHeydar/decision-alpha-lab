feat(saed-v4): implement V4-07 bounded constraint solver and action lattice

Implement the governed bridge from the V4-06 finite Treatment DSL to the V4-08 executable path outcome cube.

- freeze exact-versioned finite parameter-domain and solver-policy contracts
- verify immutable V4-06 package, handoff, policy, domain and request lineage
- enumerate bounded candidate products deterministically with count-based budgets
- instantiate canonical components and rehash identity-affecting mutations
- evaluate frozen parameter, state-guard and capability constraints
- defer known-time feature predicates without guessing or future leakage
- emit feasibility certificates, pruning ledger and complete exposure accounting
- preserve mandatory first-class Skip and Abstain non-executing fallbacks
- construct a deterministic DAG with adjacent one-parameter and fallback edges
- prohibit preference, outcome, model, selection, sizing, runtime and order semantics
- add replay, semantic diff, partitioning, telemetry, integrity and V4-08 handoff artifacts
- add closed schemas, golden/negative/conformance fixtures and mutation-heavy tests
- add diagnostic-only MQL5 mirrors with static safety validation
- add extensive Obsidian phase delivery and atomic concept documentation
- add QA, release, hash-ledger, installation and rollback tooling

Evidence remains reference-only. MetaEditor compilation, external reproduction, V4-08 path outcomes, real alpha and production authorization remain pending.
