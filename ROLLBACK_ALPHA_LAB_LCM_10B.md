# Rollback

Use Git to revert the LCM-10B commit or restore paths listed in `LCM_10B_FILE_INDEX.txt`. Confirm the repository returns to the accepted LCM-10A handoff, then rerun LCM-10A package verification and tests. Generated registries must be rolled back with code, schemas, documentation and reference bindings as one atomic unit.
