# Install Phase 11

Extract the ZIP at repository root after Phases 01 through 10 are present. The patch is additive except for the intentional `pyproject.toml` package-list update.

Run the Phase 11 tests and then the repository engineering-policy preflight before committing.
