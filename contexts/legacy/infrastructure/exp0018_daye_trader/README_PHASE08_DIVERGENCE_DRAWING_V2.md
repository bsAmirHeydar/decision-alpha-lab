# EXP0018 Phase 08 — Divergence Drawing v2

Phase 08 consumes immutable accepted Phase 07 uses and projects trend lines onto open charts of the Hunter broker symbol. It owns chart objects only. It does not detect hunts, confirm signals, retire references, map trade direction, size risk, or place orders.

Run:

```powershell
.\contexts\legacy\infrastructure\exp0018_daye_trader\powershell\run_exp0018_phase08_divergence_drawing_v2_checks.ps1 -RepoRoot "."
```

Compile `mql5/Experts/DayeTrader/EXP0018_Daye_Divergence_Drawing_Anatomy.mq5` in MetaEditor and require zero errors and zero warnings.
