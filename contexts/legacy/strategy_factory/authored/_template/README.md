# Context Package Template

Copy only through `python -m src.engine.tooling.strategy_factory.acl_os.scaffold_context`. Replace every `REPLACE_ME` value, add positive/negative/ambiguous fixtures, and run the validator. Authored files are source; `generated/` is compiler-owned.
