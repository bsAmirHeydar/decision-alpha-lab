# RTHP Context Tests

Run `python -m pytest -q tests/legacy/strategy_factory/v1/rthp_context`. The suite validates package completeness, detector semantics, fixture parity, lifecycle transitions and the MQL5 authority boundary.
