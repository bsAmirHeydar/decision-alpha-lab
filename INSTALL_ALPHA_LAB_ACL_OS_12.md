# Install ACL-12 Security Hardening Patch

Expand the root-relative patch into the repository root. Stage only the paths listed in `ACL_OS_12_FILE_INDEX.txt`. Run direct ACL-12 tests, ACL-11 regression tests and the output verifier before relying on the reference package.

This patch contains no production key material, no runtime activation and no capital authority.
