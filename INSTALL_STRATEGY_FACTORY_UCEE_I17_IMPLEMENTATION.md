# Install UCE-I17

Expand the patch at repository root, stage only paths in `UCEE_I17_FILE_INDEX.txt`, run the I17 and cumulative test suites, then compile the diagnostic MQL5 files locally in MetaEditor. Real activation remains blocked until at least two contexts have valid prospective promotion evidence.
