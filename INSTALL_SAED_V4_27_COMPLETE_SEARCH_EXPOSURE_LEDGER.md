# Installation

Expand the ZIP at the repository root, remove the ZIP, run full QA and delivery validation, stage exactly the file index, inspect the staged diff, commit with `COMMIT_MESSAGE.md`, then push.

The patch is additive except for the shared root commit-message file. Static MQL5 validation is not a substitute for Windows MetaEditor compilation.
