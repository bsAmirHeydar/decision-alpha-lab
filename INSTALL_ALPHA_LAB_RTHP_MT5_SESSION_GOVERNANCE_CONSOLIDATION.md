# Installation

Apply this ZIP from the repository root with the governed PowerShell block supplied with the release.

The installer expects the uncommitted adapter 1.0.2 clock-skew hotfix paths left by the prior full-suite failure. It removes only the superseded untracked 1.0.2 release metadata, overlays this consolidation, runs the complete relevant regression suite, stages exact paths, commits, and pushes.
