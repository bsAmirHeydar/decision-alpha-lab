# Historical release integrity amendments

This directory is append-only. An amendment is required only when an artifact classified as immutable by the active historical-release integrity policy must be corrected.

Active implementation files are not permanently hash-locked to an old patch snapshot. They remain governed by current tests, schemas, semantic freezes and current-stage verifiers.
