# Wave Execution Runbook

Wave processing is non-compensatory. Each identity is admitted, evaluated and dispositioned independently. A passed Context cannot offset another Context's failed or unknown mandatory gate. Wave closure records migrated and blocked members explicitly and retains the complete packet set.
