# ACL-OS Registry

This registry contains public, versioned contracts and policies for the Alpha Lab Context Lifecycle OS. Runtime code imports contracts by version; it may not infer them from documentation. Schemas are closed by default. Changes follow semantic versioning and require compatibility and migration evidence.
