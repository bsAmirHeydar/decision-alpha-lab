# Blocked Context Re-entry Runbook

A blocked Context may re-enter migration only through a new bounded patch that binds the exact LCM-08C packet digest, resolves every listed blocking reason with evidence, reruns characterization, authors a canonical package, executes hard parity, and updates the closure registry without rewriting historical records. Re-entry does not authorize consumer cutover.
