"""LCM-01 forensic repository survey.

The package is intentionally non-destructive. It reads the byte-exact LCM-00
baseline, emits deterministic forensic metadata, and denies semantic or runtime
authority.
"""

PHASE_ID = "LCM-01"
CLAIM_CEILING = "FORENSIC_SURVEY_REFERENCE_ONLY"
SCANNER_VERSION = "1.0.0"
