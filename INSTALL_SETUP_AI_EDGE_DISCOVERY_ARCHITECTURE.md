# Install

Extract this patch into the repository root. It adds a documentation/module tree and does not overwrite UCEE core code. Open the new folder as part of the existing Obsidian vault or navigate from its `00_START_HERE/00_Home.md`.

The `_machine_templates` files are starter declarations; validate them against current V3 schemas before executable use.
