# Installation

Place `ALPHA_LAB_LCM_01_BIDIRECTIONAL_EOL_INTEGRITY_HOTFIX_PATCH.zip` in the repository root, expand it, remove the archive, and execute `APPLY_ALPHA_LAB_LCM_01_BIDIRECTIONAL_EOL_HOTFIX.ps1`.

The script verifies the package and installation before staging, committing, or pushing.
