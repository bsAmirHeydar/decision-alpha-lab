# Install Alpha Lab LCM-12A

Apply the patch from the repository root. Verify the ZIP SHA-256, expand atomically, remove the ZIP, validate the patch ledger, schemas, source module and mapping package, run the LCM-12A tests and bounded regression suites, stage only `LCM_12A_FILE_INDEX.txt`, verify the exact staged path set, commit with `COMMIT_MESSAGE_LCM_12A.md`, and push.

The patch performs no document move, rename or deletion.
