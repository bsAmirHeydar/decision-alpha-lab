# LCM-05 MQL5 static contract mirror

These headers mirror LCM-05 topology, path, dependency and authority contracts. They do not materialize paths, migrate files, submit orders or activate capital. MetaEditor compilation and MT5 runtime parity are not claimed by this Linux reference build.
