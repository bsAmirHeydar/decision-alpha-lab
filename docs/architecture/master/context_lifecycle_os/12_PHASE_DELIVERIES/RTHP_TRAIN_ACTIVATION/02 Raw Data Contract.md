# Raw Data Contract

The activation accepts two local, causally ordered BID tick JSONL artifacts. Every tick carries explicit event and known time, symbol, price, tick size, and optional source sequence. The configuration freezes provider, entitlement, source revision, contract-roll policy, timezone, price basis, and availability policy. Network fetches and inferred source metadata are prohibited.
