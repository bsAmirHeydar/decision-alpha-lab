# One-Shot Command

From the repository root, set `PYTHONPATH` to `lab/11_strategy_factory/python`, validate the run-specific configuration, execute the `run` command, and verify the resulting run directory. The command performs every remaining context-side step automatically.
