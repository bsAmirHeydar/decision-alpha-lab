# Local recovery is not remote disaster recovery

A byte-exact temporary-workspace round trip proves bounded local recovery only. Network, credentials, remote refs and machine rebuild require separate operational evidence.
