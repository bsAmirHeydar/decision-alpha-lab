# Hostile Review

The review attempts to falsify readiness through static-name collision, cross-instance collision, broad deletion, non-host symbol projection, current-bar-as-closed-bar representation and backfill/live divergence. Canonical collisions are a hard failure. Legacy risks and unavailable runtime evidence remain blockers or UNKNOWNs. Aggregate success never overrides a non-compensatory blocker.
