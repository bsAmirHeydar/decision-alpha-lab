# Canonical Event Contract

Each visual event carries deterministic event identity, visual identity, instance, chart, symbol, timeframe, availability time, anchors, semantic payload and lifecycle action. The record is sufficient to rebuild a projection without consulting chart state as source truth.
