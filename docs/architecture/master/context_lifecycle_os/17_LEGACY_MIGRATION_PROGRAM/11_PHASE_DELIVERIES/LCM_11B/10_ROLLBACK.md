# Rollback

Rollback removes only LCM-11B registry, reference adapters, fixtures and harness artifacts listed by the exact file index. Legacy visual source remains available and requires no reconstruction.
