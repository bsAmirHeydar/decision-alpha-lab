# Exact Version Pinning

Every compatibility record pins the canonical target by SHA-256 content digest. Floating `latest`, implicit aliases and version inference are forbidden. The warning and redirect contract expose the same exact successor.
