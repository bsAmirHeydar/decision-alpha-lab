# Compatibility Warning Catalog

All 613 identities receive deterministic actionable guidance containing both legacy and canonical locators. Warning metadata is delivered before resolution and cannot change behavior or emit runtime side effects.
