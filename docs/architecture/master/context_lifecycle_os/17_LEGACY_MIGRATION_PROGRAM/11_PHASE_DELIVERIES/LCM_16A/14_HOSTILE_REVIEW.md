# Hostile review

Hostile cases include silent rebaseline, amendment outside AIEOS scope, old/new hash mismatch, duplicate amendment path, missing LFS objects, static scan mislabeled as compilation, Python parity mislabeled as terminal parity, secret fixture false positives, and authority flags flipped true.

All deterministic hostile assertions are fail-closed.
