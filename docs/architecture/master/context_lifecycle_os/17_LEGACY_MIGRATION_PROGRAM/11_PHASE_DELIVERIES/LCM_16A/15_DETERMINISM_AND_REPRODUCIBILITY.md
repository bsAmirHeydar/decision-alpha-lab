# Determinism and reproducibility

Machine identities and digests exclude wall-clock values. Baseline amendments are rebuilt from the LCM-15C lock ledger and current bytes. The suite matrix records exact command, working directory and Python path requirements. External tool evidence must include environment identity and logs before it can alter status.
