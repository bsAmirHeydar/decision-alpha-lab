# JSON, YAML and schema validation

Every repository JSON and YAML document is syntax-parsed. Every `*.schema.json` document is checked with its declared validator class. Syntax validity is not overstated as instance-to-schema conformance where no deterministic artifact-to-schema locator exists.
