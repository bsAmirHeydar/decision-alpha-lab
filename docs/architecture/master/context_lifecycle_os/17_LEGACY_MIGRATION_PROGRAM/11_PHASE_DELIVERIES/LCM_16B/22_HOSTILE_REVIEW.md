# Hostile review

The hostile suite covers upstream digest tampering, path traversal, symlink escape, restore mismatch, LFS pointer overclaim, dirty compiler logs, missing compile targets, incomplete tester evidence, terminal parity mismatch, unresolved external consumers, missing approvals, compensatory gate logic, certificate over-issuance, authority escalation and deletion broadening.

Every case is fail-closed.
