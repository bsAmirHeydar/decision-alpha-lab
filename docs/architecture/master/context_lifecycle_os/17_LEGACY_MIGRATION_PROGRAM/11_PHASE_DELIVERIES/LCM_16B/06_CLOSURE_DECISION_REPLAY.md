# Closure decision replay

The negative replay proves that five UNKNOWN dimensions plus missing approvals produce `BLOCKED` and no certificate. The positive synthetic replay proves that the policy can produce `ACCEPTED` only when every mandatory dimension is PASS and all separation-of-duties approvals are present.

Synthetic replay validates policy behavior only. It is never imported as real external evidence.
