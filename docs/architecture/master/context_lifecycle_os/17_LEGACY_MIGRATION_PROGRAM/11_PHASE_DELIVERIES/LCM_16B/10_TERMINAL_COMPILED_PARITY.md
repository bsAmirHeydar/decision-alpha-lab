# Terminal-compiled parity

The parity gate requires a positive case count, zero mismatches and a hash-bound report produced from terminal-compiled artifacts. Python-to-Python and source-mirror tests remain reference parity and cannot substitute for the terminal dimension.
