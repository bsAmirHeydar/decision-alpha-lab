# Upstream binding

LCM-16B binds both the canonical object digest and exact file digest of `LCM16A_TO_LCM16B_HANDOFF.json`. The handoff must remain `BLOCKED`, list the five mandatory external dimensions and preserve all authority flags as false.

A mutated upstream handoff, a different audit identity or an inferred PASS from static evidence fails before the recovery drill begins.
