# Installation

Apply from the repository root using the PowerShell block supplied with the ZIP. The block expands the archive, removes it, validates hashes, schemas, static boundaries and package contracts, runs LCM-10B and upstream regression tests, stages exact indexed paths, commits and pushes.
