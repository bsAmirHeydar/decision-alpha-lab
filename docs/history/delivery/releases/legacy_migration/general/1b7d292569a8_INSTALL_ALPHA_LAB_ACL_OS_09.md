# Install ACL-09 Patch

Expand the patch at the repository root. Run ACL-09 tests and direct ACL-08 regression tests. The included reference fixture is synthetic and proves mechanics only.

Rollback is performed by reverting the ACL-09 commit or selecting the prior immutable memory snapshot. Do not delete or rewrite published memory events.
