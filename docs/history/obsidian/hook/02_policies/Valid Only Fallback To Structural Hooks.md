# Valid Only Fallback To Structural Hooks

When valid-only mode selects zero Hooks, the chart should not become blind during active research.

`InpHookPhase02ValidOnlyFallbackToStructural=true` allows the renderer to draw structural Hook candidates as a visual fallback without promoting them into valid Hook families.
