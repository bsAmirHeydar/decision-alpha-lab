# Phase 43 Canonical Valid Visible Set

This architecture step introduces a narrow production-visible selector:

```text
valid_after_opposing_f3 OR valid_after_hook
```

It is a rendering-layer selector, not a counting-layer selector.

Hook counting remains complete; rendering becomes strict.
