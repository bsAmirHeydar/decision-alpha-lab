# Force All Visual Objects On Mode

`InpForceAllVisualObjectsOn` is a defensive visual setting. It prevents old saved EA inputs from leaving newly introduced drawing flags disabled. In visual-audit mode, every drawing component is forced on while chart comments remain off.
