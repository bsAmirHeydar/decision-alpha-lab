# Future Optimization Version Gate

Future versions may allow the model to optimize or suggest strategy changes, but only after:

1. enough statistics exist;
2. the base version is measured;
3. recommendations are reviewed;
4. the strategy architect approves;
5. the optimized version is tested separately.

The gate is closed in the current version.
