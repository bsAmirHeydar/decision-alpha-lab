# Raw Execution Field Before Statistical Constraints

The raw execution field is the complete set of confirmed and non-invalidated divergences before caps, filters, rankings, or exposure limits.

## Purpose

It lets the strategy discover its real behavior before optimization.

## Later constraints

Potential constraints must be promoted only after evidence.
