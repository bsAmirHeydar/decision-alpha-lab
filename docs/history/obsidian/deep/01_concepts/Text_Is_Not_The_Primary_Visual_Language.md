# Text Is Not The Primary Visual Language

Text labels are useful for debugging but destructive for chart reading when many CGs are active. EXP0017 now treats text as a full-audit option, not the default visual layer.
