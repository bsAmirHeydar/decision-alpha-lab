# Exact M1 Extreme Time For Drawing

Hotfix002 computes exact visual extreme time on demand using M1 data.

For high-side states it finds the maximum high. For low-side states it finds the minimum low.

If M1 lookup is not available, drawing falls back to configured cycle-boundary or confirmation time.
