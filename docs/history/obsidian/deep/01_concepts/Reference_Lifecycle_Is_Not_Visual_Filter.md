# Reference Lifecycle Is Not A Visual Filter

This rule is enforced before drawing, inside confirmation/lifecycle logic. Visual output only receives eligible signals. The drawing layer must not decide reference validity by itself.
