# Role Switch Is Reference Compromise

If the former protected symbol later appears as hunter for the same reference side, the reference side is considered compromised. The EA suppresses that signal instead of drawing a new divergence from a stale reference.
