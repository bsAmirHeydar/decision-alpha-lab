# Origin And Destination Inputs As Visual Control

Origin and destination are controlled independently by symbol, time and price.

This allows the strategy architect to choose whether a drawing should use exact wick times, cycle boundaries, confirmation close, hunter prices or clean-symbol comparison values.
