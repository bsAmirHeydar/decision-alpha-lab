# Reference Readiness State

A reference pair is ready only when both symbols have valid M1 high/low data for the same CG cycle window.

One-sided missing data does not become a clean-symbol condition.
