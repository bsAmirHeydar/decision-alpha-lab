# Chart Symbol Scale Caveat

A chart can only render objects on its own price scale, so drawing non-chart-symbol prices may be visually misleading unless hunter-chart-only mode is enabled.

## Phase 06 implication

This concept keeps visual audit and raw event persistence separate from execution and performance interpretation.
