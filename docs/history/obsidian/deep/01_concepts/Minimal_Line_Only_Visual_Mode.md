# Minimal Line-Only Visual Mode

The default Phase 06 visual state is now line-first and text-free. The chart should show divergence geometry, not paragraphs of signal metadata.

Minimal mode draws only the origin-to-destination leg for each confirmed divergence on the symbol-local chart.
