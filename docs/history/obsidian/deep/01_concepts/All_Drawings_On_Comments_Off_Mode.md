# All Drawings On Comments Off Mode

The default requested operating mode is all visual objects enabled while the chart Comment panel is disabled. Labels remain chart objects, not comments.

## Doctrine

```text
visual representation != strategy mutation
```
