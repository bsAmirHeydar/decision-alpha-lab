# Historical Drawing Is Not Ledger By Default

Historical drawing is enabled by default, but historical ledger writing is disabled by default. This prevents the first visual validation phase from flooding the CSV audit ledger. Ledger backfill can be enabled after visual correctness is confirmed.
