# Phase06 Hotfix007 Reference Lifecycle Map

```text
one-sided divergence
  -> identify clean/protected symbol
  -> activate reference-side lifecycle key
  -> allow repeats while same protected symbol survives
  -> if protected hunts reference side
       retire reference side
       suppress future divergence from that reference side
```
