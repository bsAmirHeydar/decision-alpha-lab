# Phase 12.5 Fixture Contract

This directory is reserved for small deterministic CSV fixtures used to test:

- missing required columns;
- duplicate sample or outcome IDs;
- orphan Phase 11 predictions;
- broken fold chronology;
- inconsistent label semantics;
- writer-summary count mismatch;
- a fully reconciled happy path.

Production data must not be committed here.
