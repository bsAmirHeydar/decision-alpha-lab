# Lifecycle Retirement

Each pulse marks currently observed events active. Previously active identities absent from a completed pulse are retired with reason `candidate_absent_on_completed_pulse`. Retirement is evidence only; it does not close positions or mutate model state.
