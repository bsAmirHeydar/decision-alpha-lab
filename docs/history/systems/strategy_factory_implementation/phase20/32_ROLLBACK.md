# Rollback

Rollback disables the SF20 plugin selection and restores the standalone EXP0017 expert. No legacy files are overwritten. Canonical ledgers remain append-only and must be retained with the failed generation and reason.
