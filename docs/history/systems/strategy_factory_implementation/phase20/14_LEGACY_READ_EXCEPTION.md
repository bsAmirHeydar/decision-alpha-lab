# Legacy Read Exception

For parity, the pilot invokes legacy classes that perform their established terminal-series reads. The shared market service is refreshed first and acts as readiness boundary. The dual-read seam is temporary, monitored, and must be removed only after an exact shared-service reimplementation passes differential replay.
