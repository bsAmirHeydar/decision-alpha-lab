# Symbol Normalization

Phase 20 preserves broker symbols exactly because suffixes and aliases are terminal-specific. A future symbol-map layer may canonicalize economic instruments, but the adapter must retain original terminal symbols in source evidence and must reject identical pair members.
