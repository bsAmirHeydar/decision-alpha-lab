# Outcome Binding

Outcome resolution requires subsequent price observations. The stage is therefore GATED with `AWAITING_CAUSAL_PRICE_PATH` at event time. Research harnesses may later attach virtual outcomes without changing the anatomy event.
