# Operator Runbook

Attach the pilot host to a chart, verify both symbols are selectable, inspect initialization logs, confirm `Live authority is disabled`, observe event IDs and candidate counts, and compare the current run/generation with the integration manifest. Stop on any parity mismatch or repeated mapping failure.
