# Differential Replay

Each legacy candidate is mapped through the same deterministic mapper used by the runtime plugin. The comparator checks counts, identities, symbols, directions, prices, timeframe, cluster, and source lineage. Any missing, extra, or mismatched event fails the case.
