# Failure Modes

Critical failure classes include symbol history skew, DST boundary disagreement, unstable source identity, duplicate emissions, missing retirements, legacy/canonical count mismatch, changed clean/hunter semantics, accidental model substitution, and broker-authority leakage. Every class has a fail-closed response.
