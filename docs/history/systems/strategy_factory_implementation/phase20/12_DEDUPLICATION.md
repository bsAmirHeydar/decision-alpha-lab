# Deduplication

A persistent legacy divergence may be observed on every timer pulse. The dedup registry emits the canonical event once per stable event identity. Later pulses create ACTIVE lifecycle evidence and increment duplicate-suppression telemetry but do not enqueue another anatomy event.
