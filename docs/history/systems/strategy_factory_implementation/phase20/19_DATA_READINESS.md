# Data Readiness

Both symbols require synchronized M1 history. Missing reference ranges, missing current ranges, stale series, and shared-service refresh failures produce no event. “No event” must be distinguishable from “no divergence”; group evidence records missing-data and symmetric-hunt counts.
