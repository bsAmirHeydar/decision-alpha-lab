# Inference Gate

The adapter must never substitute the Phase 15 reference model for an EXP0017 model. Until a governed model release matches the EXP0017 feature schema and adapter lineage, inference status is GATED with `NO_GOVERNED_EXP0017_MODEL`.
