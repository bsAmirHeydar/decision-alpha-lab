# ADR 0206 — No UI Before Runtime Telemetry

A lightweight MT5 panel is useful, but it is deferred until the runtime exposes stable health and decision telemetry. UI cannot become the owner of strategy or risk state.