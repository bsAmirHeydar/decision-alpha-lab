# Failure Modes, Negative Tests, and Recovery

Negative tests cover duplicate identities, unavailable dependencies, invalid feature indices, unsupported tasks, incompatible missingness, nonnegative-feature violations, corrupt serialized state, final-test explanation leakage, resource rejection, and missing classical families.

Failures remain in trial and benchmark evidence. Recovery is explicit: install the exact dependency, change the declared algorithm set, repair the dataset contract, or create a new report. A failed record is never edited into success.

Optional failures are clean only when typed, isolated, and represented in the report. A raw import crash or silent omission fails the gate.
