# Test Strategy and Acceptance Evidence

Tests cover catalog identity, dependency probing, deterministic built-in baselines, universal-orchestrator execution for logistic, tree, forest, Naive Bayes, ridge, and Huber models, final-test explanation rejection, coefficient-stability aggregation, benchmark construction, gate blocking, closed schemas, no-authority checks, vector verification, Python compilation, MQL5 static validation, and cumulative I01-I08 regression.

Local MetaEditor compilation remains required because the current Linux environment cannot execute MetaEditor. Clean compilation must be proven by retained Windows logs.
