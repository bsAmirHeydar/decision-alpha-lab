# Mission, Scope, and Non-Goals

This phase establishes a broad transparent tabular suite that every context must beat before ranking, survival, distributional, deep sequence, vision, graph, regime, or policy learners are admitted. The suite provides mandatory naive, manual, linear, and tree references; deterministic null models; calibrated or explicitly uncalibrated probability outputs; structured dependency evidence; fold-aware explanations; and a cross-context benchmark contract.

The phase does not claim that any algorithm has trading edge, does not perform unrestricted hyperparameter search, does not promote a model, and does not create live execution authority. UCE-I11 owns search, UCE-I12 owns statistical promotion, UCE-I14 owns runtime compilation, and UCE-I18 owns production release.

A classical benchmark is an epistemic control. A context that appears valuable only against a weak or missing comparison floor is not eligible for escalation. Manual setup logic remains a first-class frozen baseline rather than being erased by AI.
