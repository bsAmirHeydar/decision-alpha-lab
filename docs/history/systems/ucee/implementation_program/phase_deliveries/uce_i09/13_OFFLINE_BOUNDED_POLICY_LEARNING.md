# Offline Bounded Policy Learning

The bounded-policy task learns only over a finite, declared action set generated by treatment compilation. It is not an unconstrained reinforcement-learning environment. Each logged row contains state features, logged action, reward, propensity, allowed actions, cluster identity, and known time.

Conservative policy improvement estimates action utility and subtracts uncertainty. A candidate action must be observed, allowed, sufficiently supported, and better than baseline by a configured lower confidence bound. Otherwise the result is baseline or abstention.

The engine forbids unseen action generation, hidden reward shaping, future-dependent state, and evaluation without logged-policy comparison. Offline value estimates are research evidence, not direct live authority.
