feat(gartal-terminal): implement stage 06 runtime filter engine

Implement the sixth coding stage for gartal terminal. Add runtime dashboard filter engine with clickable impact, special-event, currency, and utility filter controls. Add GartalNewsFilters module for mutable GT_FilterState handling, CSV currency normalization, click routing, preset reset, red-only mode, all-impact mode, all-currency mode, runtime diagnostics, and store metric refresh after each accepted click. Update dashboard UI to render live filter buttons, currency chips, and utility controls. Update chart event handling to recalculate visible events and repaint dashboard/timeline after filter changes. Add Stage 06 inputs, runtime diagnostics fields, dashboard height adjustments, specification, validation script, canvas, patch docs, and English Obsidian implementation documentation.
