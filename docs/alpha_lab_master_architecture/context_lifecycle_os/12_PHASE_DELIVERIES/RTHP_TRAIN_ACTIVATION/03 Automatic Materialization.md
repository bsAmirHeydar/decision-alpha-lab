# Automatic Materialization

The activation automatically constructs versioned New York-time cycles, relation candidates, touch states, synchronized M15 confirmations, confirmed RTHP occurrences, reference lifecycle states, cycle-instance records, and Hunter/Protected price paths. It then emits the four required ledgers and a resolved real-data binding with local URIs and SHA-256 digests.
