# Operational Blockers

A run fails closed when source files are absent, symbols are identical, time ordering is invalid, tick sizes change without a versioned adapter, no confirmed occurrence is materialized, no mature label exists, required split roles cannot be populated, a selected task is unknown, an output directory already exists, or any shared/canonical prerequisite is missing.
