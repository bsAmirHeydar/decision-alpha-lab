# RTHP Engine Invariance and Operational Handoff

## Invariance requirement

This integration is accepted only when the central engine hash set is identical before and after installation. The protected scope includes shared Context SDK, dataset, trainers, onboarding, experiments, contracts, economics, promotion, policy, runtime, ACL tools, and ACL registries.

## Allowed additive paths

- RTHP context-owned Python plugin;
- UCE-I16 generated RTHP scaffold and AI-input contracts;
- RTHP registry schemas and registration;
- RTHP-specific tests and documentation;
- patch QA, hashes, inventory, and installation material.

The canonical semantic Context root is also unchanged so that ACL-03 source approval and deterministic recompilation remain valid.

## Acceptance evidence

- package conformance with zero findings;
- deterministic observations and features;
- all registered views and clusters validated;
- schemas and catalog digests verified;
- UCE-I16 scaffold deterministic;
- final test sealed in the compiled tournament template;
- ACL-02, ACL-03, Context, UCE-I02, and UCE-I16 regressions pass;
- central engine extended snapshot unchanged;
- real train correctly blocked on external data rather than silently using fixtures.

## Operational next action

Resolve the real data binding without changing package semantics or engine code. Then run the same preflight against the immutable real occurrence ledger and binding. On PASS, provide the activation manifest to the existing ACL-05 batch builder.
