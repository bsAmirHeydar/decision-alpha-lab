# Artifact Map

The phase publishes machine registries, JSONL record ledgers, 613 per-consumer redirect contracts, reference-scan snapshot, policies, schemas, deterministic Python implementation, tests, acceptance and hostile-review reports, rollback manifest, output manifest and the LCM-14B handoff.
