# Program closure certificate

The certificate is currently `NOT_ISSUED` and claims no program closure. An `ISSUED` certificate may claim only `LEGACY_MIGRATION_PROGRAM_REFERENCE_CLOSED` and still creates no trading, runtime, broker or capital authority.

The certificate digest binds the evaluated decision. It cannot be edited independently from its gates.
