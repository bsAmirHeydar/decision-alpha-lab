# Rollback

Rollback removes only LCM-16B-owned code, schemas, policies, tests, package artifacts and delivery notes, then restores the three bounded roadmap documents from the parent commit. Legacy source, canonical targets, redirects, quarantine evidence, LFS objects and runtime state are outside rollback scope.
