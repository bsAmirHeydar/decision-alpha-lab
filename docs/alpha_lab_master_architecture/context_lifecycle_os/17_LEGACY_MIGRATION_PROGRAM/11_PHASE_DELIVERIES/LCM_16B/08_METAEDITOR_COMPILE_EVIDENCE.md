# MetaEditor compile evidence

All six representative targets frozen by LCM-16A must be compiled. Every target requires the source identity, clean compiler log, `0 errors, 0 warnings`, terminal/MetaEditor build identity and the resulting EX5 hash.

Static MQL5 compatibility, Python parsing and presence of source files cannot satisfy this gate.
