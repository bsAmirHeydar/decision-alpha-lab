# Closure test matrix

Each gate is non-compensatory. PASS counts cannot override a mandatory FAILED, BLOCKED or UNKNOWN dimension. The matrix separates artifact validation from the program closure decision: the LCM-16A package can be internally valid while the migration closure decision remains blocked.
