# MQL5 compile matrix

Static compatibility scanning covers all `.mq5` and `.mqh` sources. Representative high-risk families are listed for real MetaEditor compilation. The Linux audit environment has no MetaEditor executable, terminal build or compiler log, therefore real compile status remains `UNKNOWN`.

Static Python/MQL5 mirrors are never accepted as compiler evidence.
