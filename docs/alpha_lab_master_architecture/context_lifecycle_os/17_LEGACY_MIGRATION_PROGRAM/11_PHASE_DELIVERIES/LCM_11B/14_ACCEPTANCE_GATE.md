# Acceptance Gate

All 128 surfaces have canonical visualizer contracts. Eligible surfaces pass isolation, restart and semantic parity; blocked surfaces are explicit. Cutover is exact and reversible, and all authority counters are zero.
