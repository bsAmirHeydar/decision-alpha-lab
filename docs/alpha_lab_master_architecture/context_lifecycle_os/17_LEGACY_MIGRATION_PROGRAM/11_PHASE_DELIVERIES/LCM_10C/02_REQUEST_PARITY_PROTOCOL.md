# Request Parity Protocol

Parity compares side, symbol, entry, stop, targets, volume, expiry, reason codes and lifecycle transitions. A rejection or no-request outcome is a first-class parity result. Missing legacy runtime traces are recorded as UNKNOWN rather than inferred.
