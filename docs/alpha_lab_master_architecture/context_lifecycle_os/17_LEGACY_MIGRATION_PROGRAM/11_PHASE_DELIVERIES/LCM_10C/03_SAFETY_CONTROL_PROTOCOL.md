# Safety-Control Protocol

Duplicate identity, stale quote, excessive spread, tick alignment, volume constraints, stop distance, session state, missing quote, reconciliation mismatch and kill switch are non-compensatory. Any triggered control rejects before any submission capability is consulted.
