# Python Research Mirror Boundary

The Python package in Phase 02 mirrors runtime configuration, state transitions and audit bus semantics for engineering tests. It does not become a second live runtime.

Python responsibilities later include advanced statistical analysis, anti-overfit validation, model training, calibration and report generation. Canonical anatomy, candidate geometry and broker transitions remain MQL5-owned.
