# Adapter Configuration

The exact configuration includes symbol pair, broker offset, New York DST policy, enabled cycle-group bitmask, M1 history requirement, lookback, pilot mode, and an immutable `live_authority=false` field. The configuration hash participates in descriptor identity and mapping evidence.
