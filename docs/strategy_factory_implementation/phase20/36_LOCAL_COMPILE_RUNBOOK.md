# Local Compile Runbook

Run `tools/strategy_factory/compile_sf20_integration.ps1` with MetaEditor. Compile the pilot host, diagnostic EA, and self-test EA. MetaEditor evidence is local-only and is not claimed by the generated QA report.
