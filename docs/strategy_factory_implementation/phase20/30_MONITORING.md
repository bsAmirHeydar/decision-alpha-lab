# Monitoring

The coordinator emits typed event-count telemetry with run, generation, strategy, model-placeholder, correlation, causation, observed time, known time, sequence, and payload hash. Alerts and lifecycle recommendations remain advisory and operator-controlled.
