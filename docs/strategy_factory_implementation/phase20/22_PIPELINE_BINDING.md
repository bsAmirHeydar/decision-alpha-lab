# Pipeline Binding

The pilot routes canonical events through the shared context engine and reference candidate matrix. It then produces explicit stage evidence. Outcome waits for subsequent causal prices. Inference is gated because no governed EXP0017 model exists. Decision, paper/shadow, and live remain gated downstream.
