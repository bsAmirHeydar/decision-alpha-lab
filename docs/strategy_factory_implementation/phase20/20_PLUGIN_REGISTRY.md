# Static Plugin Registry

The plugin identity is `sf20.exp0017.temporal_intermarket_divergence@1.0.0`. It declares multi-symbol, multi-timeframe, timer, replay, deterministic, stateful, parent-link, cluster, and synchronization capabilities. Runtime selection remains exact-version and static.
