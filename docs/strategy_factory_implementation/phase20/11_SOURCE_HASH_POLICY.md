# Source Hash Policy

The canonical source hash is identity-stable and excludes evolving current extremes. A separate legacy payload hash includes all prices and notes for differential evidence. This split preserves event idempotence without discarding audit fidelity.
