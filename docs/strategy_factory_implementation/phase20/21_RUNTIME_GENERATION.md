# Runtime Generation

The adapter descriptor hash, configuration hash, legacy source hash, run ID, and generation ID belong in the integration manifest. A changed symbol pair, cycle mask, timezone policy, or source revision requires a new immutable generation.
