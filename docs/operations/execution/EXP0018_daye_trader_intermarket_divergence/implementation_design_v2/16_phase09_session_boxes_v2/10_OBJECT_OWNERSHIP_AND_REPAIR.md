# Object Ownership and Repair

All owned rectangles use the `EXP0018_P09_BOX_` namespace. P09 may only alter or delete objects under this prefix. Foreign indicators, discretionary drawings and P08 trend lines are untouchable.

Deterministic naming supports idempotent create/verify/repair. A manually deleted box is recreated while its admitted source snapshot remains inside lookback.
