# Audit Ledger

The optional CSV records SUMMARY, PROJECTION and EVENT rows. Projection rows include snapshot and period IDs, session code, symbol identities, chart ID/timeframe, source completeness, object status, UTC and broker boundaries, symbol-local High/Low and all three causal timestamps.

Audit is disabled by default to avoid unnecessary disk I/O.
