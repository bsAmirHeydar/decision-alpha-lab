# Input and Output Schema

Inputs cover symbols, base timeframe, lookback, source-history capacity, completeness admission, chart targets, four colors, alpha, object style, ownership policy, refresh, time conversion, diagnostics and audit.

Outputs are chart rectangles, `DAYE_SessionBoxProjection` records, store summary, transition events and optional Common Files CSV rows.
