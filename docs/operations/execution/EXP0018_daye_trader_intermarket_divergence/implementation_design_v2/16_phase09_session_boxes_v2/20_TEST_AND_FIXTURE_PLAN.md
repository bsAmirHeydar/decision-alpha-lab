# Test and Fixture Plan

Golden fixtures cover complete create, closed verify, manual repair, open update, partial rejection, empty rejection, disabled session, lookback exclusion, missing chart, manual-delete recreation, orphan cleanup and symbol-local scale isolation.

Embedded tests cover config, canonical session registry, deterministic names and default completeness admission. Static validation checks required modules, object APIs, ownership prefix and absence of trading/network capabilities.
