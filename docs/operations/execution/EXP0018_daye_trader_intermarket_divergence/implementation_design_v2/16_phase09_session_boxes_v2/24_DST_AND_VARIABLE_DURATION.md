# DST and Variable Duration

Session boundaries are New York wall-time contracts. On DST transition dates, actual UTC elapsed duration may vary. P09 uses P03 window start/end UTC and does not assume every A/L/N/P session has a fixed UTC duration.

Chart geometry therefore remains consistent with the Time Kernel rather than hard-coded server hours.
