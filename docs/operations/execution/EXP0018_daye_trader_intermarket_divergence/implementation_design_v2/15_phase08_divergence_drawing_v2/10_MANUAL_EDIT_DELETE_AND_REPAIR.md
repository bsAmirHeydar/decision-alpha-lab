# Manual Edit, Delete, and Repair

Default behavior repairs moved endpoints and recreates manually deleted owned lines whenever the accepted use remains available. This prevents accidental chart manipulation from rewriting historical evidence.

Manual deletion is not interpreted as a strategy decision. A user who needs permanent visual removal must disable the renderer or explicitly enable cleanup on deinitialization.
