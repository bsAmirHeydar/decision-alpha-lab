# Source observation projection

P06 now exposes a read-only `ExportSourceObservations` method. It is a projection of P05 facts, not a transfer of lifecycle authority.

P07 matches observations by reference period instance and side, then checks only the stored protected symbol’s symbol-local hunt fact. Relationship ID is not part of reference identity and therefore does not block retirement evidence from another later relationship against the same level.
