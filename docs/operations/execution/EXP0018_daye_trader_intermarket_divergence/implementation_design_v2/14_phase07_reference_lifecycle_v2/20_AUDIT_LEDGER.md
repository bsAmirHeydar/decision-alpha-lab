# Audit ledger

Optional Common Files CSV record types:

- `SUMMARY`;
- `REFERENCE`;
- `USE`;
- `EVENT`.

Rows retain reference, use, result, observation, relationship, side, lifecycle state, use status, hunter, protected, event time, availability time, processing time, and reason code.
