# LCM-06 Framework Contract

The framework is a migration control plane, not a second ACL-OS. It validates and previews bounded operations. It cannot mutate source evidence, move or delete sources, materialize target paths, waive hard parity mismatches, enable runtime execution or activate capital.
