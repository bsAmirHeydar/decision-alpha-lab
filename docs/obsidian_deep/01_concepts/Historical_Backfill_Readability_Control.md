# Historical Backfill Readability Control

Backfill is valuable only if readable. Hotfix006 adds a draw cap and switches the default output to lines only so historical scan does not flood the chart with labels.
