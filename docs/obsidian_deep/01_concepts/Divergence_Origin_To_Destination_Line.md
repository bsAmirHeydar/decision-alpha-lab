# Divergence Origin To Destination Line

The primary visual primitive is an origin-to-destination line.

Default sell line:

```text
hunter reference high -> hunter current-cycle high
```

Default buy line:

```text
hunter reference low -> hunter current-cycle low
```

The line is a visual audit object, not an order instruction.
