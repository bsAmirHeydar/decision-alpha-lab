# No Strategy Mutation — Current Version

نسخه فعلی نباید تغییر استراتژی ایجاد کند. تغییر strategy فقط در نسخه‌های آینده و پس از gate تصمیم ممکن است.

## ممنوعیت‌ها

- تغییر CG
- تغییر مرز روز
- تغییر تعریف هانت
- تغییر تایید با کلوز
- تغییر clean symbol execution
- تغییر target زمانی
- تغییر risk پایه

## علت

نسخه فعلی باید raw data تولید کند. اگر همزمان تغییر هم بدهد، داده و تصمیم با هم مخلوط می‌شوند.
