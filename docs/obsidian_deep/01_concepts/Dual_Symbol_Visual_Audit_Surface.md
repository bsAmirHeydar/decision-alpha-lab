# Dual Symbol Visual Audit Surface

The visual surface of EXP0017 is now both configured symbols, not only the attached chart. Each input symbol receives its own valid price-scale drawing package.

## Doctrine

```text
visual representation != strategy mutation
```
