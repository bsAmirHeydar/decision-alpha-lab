# Future Decisions Backlog

Some decisions are intentionally postponed.

Examples:

- whether future AI can optimize rules;
- whether CG overlap should become a filter;
- whether position limits should be introduced;
- whether some CGs should be disabled;
- whether target logic should change.

These are future-version questions, not current rules.
