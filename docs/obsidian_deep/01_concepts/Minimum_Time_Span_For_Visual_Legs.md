# Minimum Time Span for Visual Legs

If origin and destination times collapse to the same timestamp, the visual leg can become compressed and appear missing. Hotfix004 enforces a minimum span so the drawing remains visible without changing the underlying signal identity.
