# Repeated Divergence While Protected Survives

The same reference side may create repeated divergence observations only while the clean/protected symbol still has not hunted that reference side. This preserves valid repeated asymmetry without allowing stale references after breach.
