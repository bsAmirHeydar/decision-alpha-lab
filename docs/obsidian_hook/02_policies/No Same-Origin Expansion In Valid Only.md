# No Same-Origin Expansion In Valid Only

A valid Hook row must not pull all same-origin structural branches into the chart.

Same-origin expansion is useful for debugging, but it creates label leakage in production valid-only mode.

In Step 1, the final visible set is limited to selected valid rows plus required Hook-after-Hook parent companions.
