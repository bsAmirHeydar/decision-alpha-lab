# Phase 37 Hook Terminal and Valid Labels

Phase 37 aligns terminal semantics and label rendering with the final Hook doctrine.

Pipeline:

```text
seed-owned sequence rows
→ normalize Hook group terminal
→ annotate immediate F3 / Hook-after-Hook validity
→ select visible valid Hook groups
→ draw labels only for selected groups
```

Key correction:

```text
positive Hook terminal = lowest valley seen by the Hook group
negative Hook terminal = highest peak seen by the Hook group
```
