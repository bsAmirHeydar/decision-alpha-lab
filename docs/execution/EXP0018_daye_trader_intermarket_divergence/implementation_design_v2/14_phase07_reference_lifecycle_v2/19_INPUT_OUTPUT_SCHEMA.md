# Input and output schema

Lifecycle inputs explicitly expose repeat policy, exact-opportunity deduplication, protected-touch retirement, double-hunt retirement, role-switch retirement, rejected-use publication, checkpoint naming, and bounded store sizes.

The defaults encode the canonical Daye policy. Disabling required core rules causes initialization failure rather than silent semantic drift.
