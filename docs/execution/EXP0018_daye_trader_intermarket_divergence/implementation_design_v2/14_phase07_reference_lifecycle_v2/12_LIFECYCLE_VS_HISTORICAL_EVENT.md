# Lifecycle truth versus historical event truth

A reference can retire today while a signal confirmed yesterday remains historically true. These are different facts:

- lifecycle record answers whether the reference may create a future use;
- accepted use record answers whether a confirmation existed at its host close.

Retirement must never erase, move, or rewrite accepted historical use evidence.
