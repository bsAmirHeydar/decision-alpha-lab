# Store, events, and public API

Public read-only API:

- `GetCurrentSummary`;
- `ExportReferences`;
- `ExportUses`.

Key events include activation, accepted use, duplicate suppression, survival, protected-touch retirement, double-hunt retirement, role-switch retirement, rejected-after-retirement, checkpoint restore, and checkpoint save.
