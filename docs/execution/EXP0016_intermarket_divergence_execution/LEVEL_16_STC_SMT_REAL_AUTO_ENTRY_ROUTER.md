# Level 16 — STC SMT Real Auto Entry Router

Patch scope: gated real market entry from confirmed STC paper plans.

Main document:

`lab/09_execution/EXP0016_intermarket_divergence_execution/EXEC001_STC_SMT_Cycles/37_level_16_real_auto_entry_router.md`

Default safety: real auto-entry is disabled. Enable only with `STC_MODE_AUTO_TRADE` and `InpEnableRealAutoEntry=true`.
