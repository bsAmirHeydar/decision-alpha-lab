# Installation

Expand the ZIP at the repository root, remove the ZIP, run the full QA and delivery validator, stage only the indexed patch files, inspect the cached diff, commit using `COMMIT_MESSAGE.md`, and push. MetaEditor compilation remains an external Windows evidence gate and is not represented by static validation.
