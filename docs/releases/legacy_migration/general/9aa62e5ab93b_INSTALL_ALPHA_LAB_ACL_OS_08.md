# Install ACL-08

Apply the root-relative ZIP over the repository state containing ACL-07. Run the ACL-08 direct tests and ACL-07 regression suite. Generated reference outputs are included and must replay deterministically. MQL5 files are static contract mirrors only; MetaEditor and MT5 parity are not claimed.
