# Installation and Verification

Extract at repository root with overwrite enabled. Run the engineering policy, UCE-I05 boundary/static/delivery checks, Python compileall, conformance CLI, and cumulative tests. Compile the three MQL5 EAs locally with MetaEditor using `compile_uce_i05_economics.ps1`; retain compiler logs as acceptance evidence.
