# Alpha Lab ACL-OS ACL-01

`ACL-01 — Repository, Identity and Artifact Locator` is the accepted reference implementation for global artifact identity, canonical repository placement, ownership, schema/plugin registries, deterministic resolution, dependency direction, lineage, compatibility and migration governance.

The package is non-trading. It cannot submit orders, activate capital or authorize production.

Run:

```powershell
python -m tools.strategy_factory.acl_os.acl_01.run_acl_01_full_qa
python -m tools.strategy_factory.acl_os.acl_01.validate_acl_01_delivery
```
