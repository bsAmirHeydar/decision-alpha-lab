# Install UCEE-I09

1. Extract the patch at repository root.
2. Run Engineering Policy, boundary, static, delivery, vector, and pytest commands.
3. Compile the three MQL5 diagnostic/self-test experts locally with MetaEditor.
4. Review UCE-I09 acceptance evidence and residual risks.
5. Commit only after all local gates pass.

No external ranking or survival dependency is required for the native conformance suite. Optional packages must be installed through explicit extras and never trigger silent fallback.
