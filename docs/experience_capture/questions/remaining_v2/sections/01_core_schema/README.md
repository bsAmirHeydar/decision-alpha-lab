# Core NDS Schema and Lifecycle

These are remaining questions only. They do not replace or delete answered question/answer records.

## NDS-R01 — Canonical NDS Object Model

The answered records define ontology boundaries, but the exact object model is still open.

Path:

```text
docs/experience_capture/questions/remaining_v2/by_code/NDS-R01.md
```

## NDS-R02 — Cycle Lifecycle: Birth, Life, Near-Death, Death, and New Cycle

Extreme is now defined as near-death, but the complete cycle lifecycle still needs exact states.

Path:

```text
docs/experience_capture/questions/remaining_v2/by_code/NDS-R02.md
```

## NDS-R03 — Node Identity, Node Cluster, and Node Replacement

L2 and anchor nodes are defined, but node identity across time and clusters is still unclear.

Path:

```text
docs/experience_capture/questions/remaining_v2/by_code/NDS-R03.md
```
