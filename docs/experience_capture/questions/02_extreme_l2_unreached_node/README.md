# Extreme, L2 Node, and Unreached Node

This section defines the L2 unreached-node extreme. These answers will directly shape the Extreme Opportunity Dataset and Extreme Quality Model.

How to answer this section:
- Answer in your own words first; do not force a clean definition too early.
- Add rules, exceptions, and chart examples separately.
- Use the question code in every image caption or filename.
- If something is still intuitive and not formal, say so clearly.

Questions:

### EXT-01 — How is an extreme defined in relation to an unreached node?

Why this matters: The answer determines how an extreme becomes machine-readable and how the system selects the anchor node, entry area, stop, and invalidation.

Answer in these layers:
- Raw intuition: describe how you see it on the chart before formalizing it.
- Operational definition: explain how we could turn it into a field, rule, label, or policy.
- Exceptions: describe the cases where the rule fails, weakens, or changes meaning.
- Chart evidence: add examples when the idea is easier to understand visually.

Clarify these points:
- Mark the anchor node and explain why this node matters.
- Explain whether the extreme is the node itself or the zone around it.
- Describe the valid and invalid versions of this case.
- Explain how entry, stop, destination, and scenario context connect to the node.
- Give at least one good example and one deceptive example if possible.

Future system outputs affected by this answer:
- extreme_anchor_node fields
- extreme_zone fields
- node validity labels
- extreme quality features
- execution policy

Image guidance: A chart example is strongly recommended. Mark the node, extreme zone, entry, stop, destination, scenario, and scale.

Recommended answer folder: ../answers/EXT-01/

User answer:


### EXT-02 — Which project logic produces the anchor node around which the extreme is built?

Why this matters: The answer determines how an extreme becomes machine-readable and how the system selects the anchor node, entry area, stop, and invalidation.

Answer in these layers:
- Raw intuition: describe how you see it on the chart before formalizing it.
- Operational definition: explain how we could turn it into a field, rule, label, or policy.
- Exceptions: describe the cases where the rule fails, weakens, or changes meaning.
- Chart evidence: add examples when the idea is easier to understand visually.

Clarify these points:
- Mark the anchor node and explain why this node matters.
- Explain whether the extreme is the node itself or the zone around it.
- Describe the valid and invalid versions of this case.
- Explain how entry, stop, destination, and scenario context connect to the node.
- Give at least one good example and one deceptive example if possible.

Future system outputs affected by this answer:
- extreme_anchor_node fields
- extreme_zone fields
- node validity labels
- extreme quality features
- execution policy

Image guidance: A chart example is strongly recommended. Mark the node, extreme zone, entry, stop, destination, scenario, and scale.

Recommended answer folder: ../answers/EXT-02/

User answer:


### EXT-03 — Why is L2 the default node level for extremes?

Why this matters: The answer determines how an extreme becomes machine-readable and how the system selects the anchor node, entry area, stop, and invalidation.

Answer in these layers:
- Raw intuition: describe how you see it on the chart before formalizing it.
- Operational definition: explain how we could turn it into a field, rule, label, or policy.
- Exceptions: describe the cases where the rule fails, weakens, or changes meaning.
- Chart evidence: add examples when the idea is easier to understand visually.

Clarify these points:
- Mark the anchor node and explain why this node matters.
- Explain whether the extreme is the node itself or the zone around it.
- Describe the valid and invalid versions of this case.
- Explain how entry, stop, destination, and scenario context connect to the node.
- Give at least one good example and one deceptive example if possible.

Future system outputs affected by this answer:
- extreme_anchor_node fields
- extreme_zone fields
- node validity labels
- extreme quality features
- execution policy

Image guidance: A chart example is strongly recommended. Mark the node, extreme zone, entry, stop, destination, scenario, and scale.

Recommended answer folder: ../answers/EXT-03/

User answer:


### EXT-04 — Is L2 always the default, or only in specific contexts?

Why this matters: The answer determines how an extreme becomes machine-readable and how the system selects the anchor node, entry area, stop, and invalidation.

Answer in these layers:
- Raw intuition: describe how you see it on the chart before formalizing it.
- Operational definition: explain how we could turn it into a field, rule, label, or policy.
- Exceptions: describe the cases where the rule fails, weakens, or changes meaning.
- Chart evidence: add examples when the idea is easier to understand visually.

Clarify these points:
- Mark the anchor node and explain why this node matters.
- Explain whether the extreme is the node itself or the zone around it.
- Describe the valid and invalid versions of this case.
- Explain how entry, stop, destination, and scenario context connect to the node.
- Give at least one good example and one deceptive example if possible.

Future system outputs affected by this answer:
- extreme_anchor_node fields
- extreme_zone fields
- node validity labels
- extreme quality features
- execution policy

Image guidance: A chart example is strongly recommended. Mark the node, extreme zone, entry, stop, destination, scenario, and scale.

Recommended answer folder: ../answers/EXT-04/

User answer:


### EXT-05 — What exactly does unreached node mean?

Why this matters: The answer determines how an extreme becomes machine-readable and how the system selects the anchor node, entry area, stop, and invalidation.

Answer in these layers:
- Raw intuition: describe how you see it on the chart before formalizing it.
- Operational definition: explain how we could turn it into a field, rule, label, or policy.
- Exceptions: describe the cases where the rule fails, weakens, or changes meaning.
- Chart evidence: add examples when the idea is easier to understand visually.

Clarify these points:
- Mark the anchor node and explain why this node matters.
- Explain whether the extreme is the node itself or the zone around it.
- Describe the valid and invalid versions of this case.
- Explain how entry, stop, destination, and scenario context connect to the node.
- Give at least one good example and one deceptive example if possible.

Future system outputs affected by this answer:
- extreme_anchor_node fields
- extreme_zone fields
- node validity labels
- extreme quality features
- execution policy

Image guidance: A chart example is strongly recommended. Mark the node, extreme zone, entry, stop, destination, scenario, and scale.

Recommended answer folder: ../answers/EXT-05/

User answer:


### EXT-06 — If price slightly penetrates the node and quickly returns, is the extreme invalid or still valid?

Why this matters: The answer determines how an extreme becomes machine-readable and how the system selects the anchor node, entry area, stop, and invalidation.

Answer in these layers:
- Raw intuition: describe how you see it on the chart before formalizing it.
- Operational definition: explain how we could turn it into a field, rule, label, or policy.
- Exceptions: describe the cases where the rule fails, weakens, or changes meaning.
- Chart evidence: add examples when the idea is easier to understand visually.

Clarify these points:
- Mark the anchor node and explain why this node matters.
- Explain whether the extreme is the node itself or the zone around it.
- Describe the valid and invalid versions of this case.
- Explain how entry, stop, destination, and scenario context connect to the node.
- Give at least one good example and one deceptive example if possible.

Future system outputs affected by this answer:
- extreme_anchor_node fields
- extreme_zone fields
- node validity labels
- extreme quality features
- execution policy

Image guidance: A chart example is strongly recommended. Mark the node, extreme zone, entry, stop, destination, scenario, and scale.

Recommended answer folder: ../answers/EXT-06/

User answer:


### EXT-07 — If multiple L2 nodes are close to each other, which one becomes the main anchor?

Why this matters: The answer determines how an extreme becomes machine-readable and how the system selects the anchor node, entry area, stop, and invalidation.

Answer in these layers:
- Raw intuition: describe how you see it on the chart before formalizing it.
- Operational definition: explain how we could turn it into a field, rule, label, or policy.
- Exceptions: describe the cases where the rule fails, weakens, or changes meaning.
- Chart evidence: add examples when the idea is easier to understand visually.

Clarify these points:
- Mark the anchor node and explain why this node matters.
- Explain whether the extreme is the node itself or the zone around it.
- Describe the valid and invalid versions of this case.
- Explain how entry, stop, destination, and scenario context connect to the node.
- Give at least one good example and one deceptive example if possible.

Future system outputs affected by this answer:
- extreme_anchor_node fields
- extreme_zone fields
- node validity labels
- extreme quality features
- execution policy

Image guidance: A chart example is strongly recommended. Mark the node, extreme zone, entry, stop, destination, scenario, and scale.

Recommended answer folder: ../answers/EXT-07/

User answer:


### EXT-08 — How important is node freshness?

Why this matters: The answer determines how an extreme becomes machine-readable and how the system selects the anchor node, entry area, stop, and invalidation.

Answer in these layers:
- Raw intuition: describe how you see it on the chart before formalizing it.
- Operational definition: explain how we could turn it into a field, rule, label, or policy.
- Exceptions: describe the cases where the rule fails, weakens, or changes meaning.
- Chart evidence: add examples when the idea is easier to understand visually.

Clarify these points:
- Mark the anchor node and explain why this node matters.
- Explain whether the extreme is the node itself or the zone around it.
- Describe the valid and invalid versions of this case.
- Explain how entry, stop, destination, and scenario context connect to the node.
- Give at least one good example and one deceptive example if possible.

Future system outputs affected by this answer:
- extreme_anchor_node fields
- extreme_zone fields
- node validity labels
- extreme quality features
- execution policy

Image guidance: A chart example is strongly recommended. Mark the node, extreme zone, entry, stop, destination, scenario, and scale.

Recommended answer folder: ../answers/EXT-08/

User answer:


### EXT-09 — How do you recognize the quality of the anchor node?

Why this matters: The answer determines how an extreme becomes machine-readable and how the system selects the anchor node, entry area, stop, and invalidation.

Answer in these layers:
- Raw intuition: describe how you see it on the chart before formalizing it.
- Operational definition: explain how we could turn it into a field, rule, label, or policy.
- Exceptions: describe the cases where the rule fails, weakens, or changes meaning.
- Chart evidence: add examples when the idea is easier to understand visually.

Clarify these points:
- Mark the anchor node and explain why this node matters.
- Explain whether the extreme is the node itself or the zone around it.
- Describe the valid and invalid versions of this case.
- Explain how entry, stop, destination, and scenario context connect to the node.
- Give at least one good example and one deceptive example if possible.

Future system outputs affected by this answer:
- extreme_anchor_node fields
- extreme_zone fields
- node validity labels
- extreme quality features
- execution policy

Image guidance: A chart example is strongly recommended. Mark the node, extreme zone, entry, stop, destination, scenario, and scale.

Recommended answer folder: ../answers/EXT-09/

User answer:


### EXT-10 — What separates a good extreme from a deceptive extreme?

Why this matters: The answer determines how an extreme becomes machine-readable and how the system selects the anchor node, entry area, stop, and invalidation.

Answer in these layers:
- Raw intuition: describe how you see it on the chart before formalizing it.
- Operational definition: explain how we could turn it into a field, rule, label, or policy.
- Exceptions: describe the cases where the rule fails, weakens, or changes meaning.
- Chart evidence: add examples when the idea is easier to understand visually.

Clarify these points:
- Mark the anchor node and explain why this node matters.
- Explain whether the extreme is the node itself or the zone around it.
- Describe the valid and invalid versions of this case.
- Explain how entry, stop, destination, and scenario context connect to the node.
- Give at least one good example and one deceptive example if possible.

Future system outputs affected by this answer:
- extreme_anchor_node fields
- extreme_zone fields
- node validity labels
- extreme quality features
- execution policy

Image guidance: A chart example is strongly recommended. Mark the node, extreme zone, entry, stop, destination, scenario, and scale.

Recommended answer folder: ../answers/EXT-10/

User answer:


### EXT-11 — Does an extreme only make sense when entry is against the current move?

Why this matters: The answer determines how an extreme becomes machine-readable and how the system selects the anchor node, entry area, stop, and invalidation.

Answer in these layers:
- Raw intuition: describe how you see it on the chart before formalizing it.
- Operational definition: explain how we could turn it into a field, rule, label, or policy.
- Exceptions: describe the cases where the rule fails, weakens, or changes meaning.
- Chart evidence: add examples when the idea is easier to understand visually.

Clarify these points:
- Mark the anchor node and explain why this node matters.
- Explain whether the extreme is the node itself or the zone around it.
- Describe the valid and invalid versions of this case.
- Explain how entry, stop, destination, and scenario context connect to the node.
- Give at least one good example and one deceptive example if possible.

Future system outputs affected by this answer:
- extreme_anchor_node fields
- extreme_zone fields
- node validity labels
- extreme quality features
- execution policy

Image guidance: A chart example is strongly recommended. Mark the node, extreme zone, entry, stop, destination, scenario, and scale.

Recommended answer folder: ../answers/EXT-11/

User answer:


### EXT-12 — What separates your extreme from classical support and resistance?

Why this matters: The answer determines how an extreme becomes machine-readable and how the system selects the anchor node, entry area, stop, and invalidation.

Answer in these layers:
- Raw intuition: describe how you see it on the chart before formalizing it.
- Operational definition: explain how we could turn it into a field, rule, label, or policy.
- Exceptions: describe the cases where the rule fails, weakens, or changes meaning.
- Chart evidence: add examples when the idea is easier to understand visually.

Clarify these points:
- Mark the anchor node and explain why this node matters.
- Explain whether the extreme is the node itself or the zone around it.
- Describe the valid and invalid versions of this case.
- Explain how entry, stop, destination, and scenario context connect to the node.
- Give at least one good example and one deceptive example if possible.

Future system outputs affected by this answer:
- extreme_anchor_node fields
- extreme_zone fields
- node validity labels
- extreme quality features
- execution policy

Image guidance: A chart example is strongly recommended. Mark the node, extreme zone, entry, stop, destination, scenario, and scale.

Recommended answer folder: ../answers/EXT-12/

User answer:

