
# Install — RTHP MT5 Automation Obsidian Roadmap

Apply the ZIP from the repository root, verify the exact file index and SHA-256 ledger, inspect the documentation-only path boundary, stage only indexed files, commit, and push.

The PowerShell command in the delivery message performs these steps.
