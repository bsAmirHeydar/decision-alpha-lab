# Install SF06 CI Recovery

Extract at repository root, run `tools/strategy_factory/verify_repository_before_push.ps1`, commit, and push. Existing failed statuses on older commits are historical and remain unchanged; the new corrective commit should pass.
