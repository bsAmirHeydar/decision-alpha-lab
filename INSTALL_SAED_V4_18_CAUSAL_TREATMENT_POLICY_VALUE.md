# Installation

Expand the ZIP at the repository root, remove the ZIP, execute the full QA and delivery validator, stage only paths in `SAED_V4_18_FILE_INDEX.txt`, inspect the cached diff, commit with `COMMIT_MESSAGE.md`, and push. Static MQL5 validation is not MetaEditor compilation. All real-data, protected, runtime and prospective evidence remains external.
