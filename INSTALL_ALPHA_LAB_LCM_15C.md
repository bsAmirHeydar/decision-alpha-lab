# Install Alpha Lab LCM-15C

Apply the ZIP from repository root using the supplied PowerShell workflow. It verifies archive safety and SHA-256, expands and removes the ZIP, validates package evidence, schemas, static authority boundaries, installation, direct and stable regressions, stages only the exact file index and commits/pushes. No delete command is executed.
