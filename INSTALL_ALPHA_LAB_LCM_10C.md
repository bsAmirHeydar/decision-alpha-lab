# Install Alpha Lab LCM-10C

Apply the ZIP from repository root, verify SHA-256, expand, remove the ZIP, run the LCM-10C CLI checks and regression tests, stage only `LCM_10C_FILE_INDEX.txt`, commit with `COMMIT_MESSAGE_LCM_10C.md`, then push.
