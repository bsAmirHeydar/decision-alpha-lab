# ACL-11 MQL5 static contract mirror

These headers expose fail-closed contract shapes only. They do not compile or activate a runtime generation, place orders, access signing keys, or authorize capital. MetaEditor and MT5 parity remain external evidence obligations.
